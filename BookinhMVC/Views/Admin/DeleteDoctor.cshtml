@model BookinhMVC.Models.BacSi
@{
    Layout = "_AdminLayout";
    ViewBag.Active = "Doctors";
    ViewBag.Title = "Xác nhận Xóa Bác sĩ";
}

<div class="row">
    <div class="col-md-7 offset-md-2">
        <h2 class="mb-4 text-danger"><i class="fas fa-trash-alt me-2"></i>Xác nhận Xóa Bác sĩ</h2>
        <hr />

        <div class="alert alert-danger shadow-sm p-4" role="alert">
            <h4 class="alert-heading"><i class="fas fa-exclamation-triangle me-2"></i>Cảnh báo: Thao tác không thể hoàn tác!</h4>
            <p>
                Bạn có **chắc chắn** muốn xóa Bác sĩ **@Model.HoTen** khỏi hệ thống không?
                Thao tác này sẽ **không thể hoàn tác** và có thể gây lỗi dữ liệu liên quan như: **Lịch hẹn** và **Đánh giá** của bệnh nhân.
            </p>
        </div>

        <div class="card shadow-lg mb-4">
            <div class="card-header bg-warning text-dark fw-bold">
                <i class="fas fa-user-times me-1"></i> Thông tin Bác sĩ cần xóa
            </div>
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-4">Mã Bác sĩ (ID):</dt>
                    <dd class="col-sm-8">@Model.MaBacSi</dd>

                    <dt class="col-sm-4">Họ và tên:</dt>
                    <dd class="col-sm-8"><span class="badge bg-danger fs-6">@Model.HoTen</span></dd>

                    @* Giả định bạn đã Include(b => b.Khoa) trong action DeleteDoctor(int id) *@
                    @if (Model.Khoa != null)
                    {
                        <dt class="col-sm-4">Khoa/Chuyên môn:</dt>
                        <dd class="col-sm-8">@Model.Khoa.TenKhoa</dd>
                    }
                    else
                    {
                        <dt class="col-sm-4">Khoa/Chuyên môn:</dt>
                        <dd class="col-sm-8">Chưa xác định</dd>
                    }

                    <dt class="col-sm-4">Số điện thoại:</dt>
                    <dd class="col-sm-8">@Model.SoDienThoai</dd>
                </dl>
            </div>
        </div>

        <form asp-action="DeleteDoctor" asp-route-id="@Model.MaBacSi" method="post">
            @* Hidden input không cần thiết khi dùng asp-route-id, nhưng tôi giữ lại để đảm bảo *@
            <input type="hidden" name="id" value="@Model.MaBacSi" />

            <button type="submit" class="btn btn-danger btn-lg me-2">
                <i class="fas fa-trash-alt me-1"></i> **Xác nhận XÓA**
            </button>
            <a href="/Admin/Doctors" class="btn btn-secondary btn-lg">
                <i class="fas fa-times me-1"></i> Hủy bỏ
            </a>
        </form>
    </div>
</div>