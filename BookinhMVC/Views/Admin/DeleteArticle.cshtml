@model BookinhMVC.Models.Article
@{
    ViewData["Title"] = "Xác Nhận Xóa Bài Viết: " + Model.Title;
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="card shadow mb-4">
    <div class="card-header py-3 bg-danger text-white">
        <h5 class="m-0 font-weight-bold">
            <i class="fas fa-exclamation-triangle"></i> Xác Nhận Xóa Bài Viết
        </h5>
    </div>
    <div class="card-body">

        <div class="alert alert-danger" role="alert">
            Bạn có chắc chắn muốn **XÓA** vĩnh viễn bài viết sau đây không? Hành động này không thể hoàn tác.
        </div>

        <dl class="row mb-4 p-3 bg-light rounded shadow-sm">
            <dt class="col-sm-3">Tiêu đề:</dt>
            <dd class="col-sm-9 fw-bold text-primary">@Model.Title</dd>

            <dt class="col-sm-3">Tác giả:</dt>
            <dd class="col-sm-9">@Model.Author.TenDangNhap</dd>

            <dt class="col-sm-3">Danh mục:</dt>
            <dd class="col-sm-9">@Model.Category.CategoryName</dd>

            <dt class="col-sm-3">Ngày đăng:</dt>
            <dd class="col-sm-9">@Model.PublishDate.ToString("dd/MM/yyyy")</dd>

            <dt class="col-sm-3">Trạng thái:</dt>
            <dd class="col-sm-9">
                <span class="badge @(Model.IsPublished ? "bg-success" : "bg-warning text-dark")">
                    @(Model.IsPublished ? "Đã Xuất Bản" : "Bản Nháp")
                </span>
            </dd>

            <dt class="col-sm-3">Tóm tắt:</dt>
            <dd class="col-sm-9">@Model.Summary</dd>
        </dl>

        <form asp-action="DeleteArticle" method="post">
            <input type="hidden" asp-for="ArticleId" />
            <button type="submit" class="btn btn-danger btn-lg">
                <i class="fas fa-trash-alt"></i> Xóa Vĩnh Viễn
            </button>
            <a asp-action="Articles" class="btn btn-secondary btn-lg">
                <i class="fas fa-times-circle"></i> Hủy
            </a>
        </form>
    </div>
</div>